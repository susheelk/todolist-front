import {Injectable} from "@angular/core";
import {Task} from '../model'
@Injectable()
export class TasksService {

    public addTask(task: Task, success?: (task: Task) => any, fail?: (error) => any) {

    }

}
